{{#if site.keys.bannerText}}
<div class="banner">
    <a href="{{{site.keys.bannerUrl}}}">{{site.keys.bannerText}}{{#if site.keys.bannerButton}} <span class="banner_button">{{site.keys.bannerButton}}</span>{{/if}}</a>
</div>
{{/if}}
