{{!--
  Stub. At site-build time the `nav-assembler` Antora extension (in
  axoniq-library-site) overrides this partial with one product-group per
  extension component for the drawer's product/version switcher, matched via
  the same componentGlob(s) nav-manifest.js uses for the nav tree's own
  "Extensions" section. If you see this comment in the output, the extension
  did not run.
--}}
